domain: mtrifonov-design.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://mtrifonov-design.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /mondaymathwithmartin/      # place folder name if the site is served in a subfolder
